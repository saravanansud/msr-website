<?php
/* -------------------------------------------------------------------------
   PREVIEW PASSWORD — template.

   Create a file named  auth.config.php  in this same folder ON THE SERVER
   (cPanel → File Manager → public_html → + File), paste the two lines below
   into it, and replace the value with your real password:

       <?php
       return 'your-preview-password-here';

   auth.config.php is git-ignored on purpose — it must NEVER be committed,
   so your password stays only on the server (not in the public GitHub repo).
   ------------------------------------------------------------------------- */
return 'your-preview-password-here';
